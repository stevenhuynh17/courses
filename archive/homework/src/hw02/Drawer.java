package hw02;

/**
 * Drawer draws Triangles with the origin (0, 0) in the center and a width and height of 200.
 * For example, the coordinate Coord.fromXAndY(100, 0) is the far right
 * and Coord.fromXAndY(0, -100) is the bottom.
 */
public class Drawer {
    /**
     * drawTriangle draws filled triangles on a buffer (or directly to the screen).
     */
    public void drawTriangle(Triangle triangle, Color color) { }
    /**
     * render actually draws them to the screen and clears any buffered triangles.
     */
    public void render() { }
}
